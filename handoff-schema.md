# CBM Lab — AI Handoff 스키마 (v1)

계산기 페이지가 AI 코파일럿 페이지로 결과를 넘길 때 사용하는 표준 데이터 형식입니다.
이 스키마를 지키면 계산기가 몇 개로 늘어나도 코파일럿/서버 쪽 코드를 건드릴 필요가 없습니다.

## 핵심 설계 원칙

**summary 필드를 계산기가 직접 만든다.**
서버(AI 프롬프트)는 각 계산기의 필드 의미를 몰라도 됩니다. 계산기 스스로
"수입원가 계산 결과: FOB $12,450, 톤당 관세 7.5%, 중국 공급업체" 같은
한국어 한 줄 요약을 만들어 넘기고, AI는 이 요약 + 원본 data를 함께 받습니다.
→ 계산기 20개를 붙여도 코파일럿/서버 코드는 그대로입니다.

## 스키마

```json
{
  "version": 1,
  "tool": "fob-cif",
  "timestamp": "2026-08-02T10:00:00.000Z",
  "summary": "수입원가 계산 결과: FOB 단가 $3.20, 물량 3,890개, 총 landed cost $12,450 (관세 7.5% 포함), 공급업체 중국",
  "data": {
    "incoterm": "FOB",
    "currency": "USD",
    "unitCost": 3.2,
    "quantity": 3890,
    "totalLandedCost": 12450,
    "dutyRate": 0.075,
    "supplierCountry": "CN"
  }
}
```

## 필드 설명

| 필드 | 타입 | 설명 |
|---|---|---|
| `version` | number | 스키마 버전. 향후 구조 변경 시 대응용 |
| `tool` | string | 계산기 식별자 (파일명 기준, 예: `fob-cif`, `cbm`, `leadtime-calculator`) |
| `timestamp` | ISO string | 생성 시각. 코파일럿 페이지에서 30분 이상 지난 데이터는 무시 |
| `summary` | string | AI 프롬프트에 그대로 들어갈 한국어 요약 1~2문장. **계산기가 직접 작성** |
| `data` | object | 도구별 원본 수치. 사용자가 코파일럿 페이지에서 수정할 수 있도록 노출 |

## 저장 위치

`localStorage` 키: `cbmlab:ai_handoff`
(브라우저 로컬 저장이므로 서버 전송 없음 — 기존 "0건 서버 전송" 원칙과 충돌하지 않음.
서버 전송은 사용자가 코파일럿 페이지에서 "생성" 버튼을 눌렀을 때만 발생합니다.)
